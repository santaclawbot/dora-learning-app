/**
 * Photos API Tests
 * Tests for photo upload and management endpoints
 */

const express = require('express');
const request = require('supertest');
const path = require('path');
const fs = require('fs');
const { createTestDatabase, initializeTestSchema, closeDatabase } = require('./setup');

// Import routes
const photosRoutes = require('../routes/photos');

describe('Photos API', () => {
  let db;
  let app;
  const testPhotoPath = path.join(__dirname, 'test-image.jpg');
  let uploadedFilename;

  beforeAll(async () => {
    // Create test database
    db = await createTestDatabase();
    await initializeTestSchema(db);

    // Set up Express app
    app = express();
    app.use(express.json());
    app.set('db', db);

    // Mount routes
    app.use('/api/photos', photosRoutes);

    // Create a test image file (1x1 pixel JPEG)
    const testImageBuffer = Buffer.from([
      
    ]);
    fs.writeFileSync(testPhotoPath, testImageBuffer);
  });

  afterAll(async () => {
    await closeDatabase(db);
    // Clean up test image
    if (fs.existsSync(testPhotoPath)) {
      fs.unlinkSync(testPhotoPath);
    }
  });

  afterEach(async () => {
    // Clean up uploaded file if exists
    if (uploadedFilename) {
      const uploadPath = path.join(__dirname, '../uploads/photos', uploadedFilename);
      if (fs.existsSync(uploadPath)) {
        fs.unlinkSync(uploadPath);
      }
      uploadedFilename = null;
    }
  });

  describe('POST /api/photos/upload', () => {
    it('should upload a photo', async () => {
      const response = await request(app)
        .post('/api/photos/upload')
        .attach('image', testPhotoPath)
        .field('profileId', 'test-profile')
        .field('description', 'A test photo');
      
      expect(response.status).toBe(201);
      expect(response.body.success).toBe(true);
      expect(response.body.photo).toBeDefined();
      expect(response.body.photo.filename).toBeDefined();
      expect(response.body.photo.url).toContain('/uploads/photos/');
      uploadedFilename = response.body.photo.filename;
    });

    it('should return error when no file provided', async () => {
      const response = await request(app)
        .post('/api/photos/upload')
        .field('profileId', 'test-profile');
      
      expect(response.status).toBe(400);
      expect(response.body.success).toBe(false);
    });

    it('should reject non-image files', async () => {
      // Create a text file
      const textFilePath = path.join(__dirname, 'test.txt');
      fs.writeFileSync(textFilePath, 'This is not an image');

      const response = await request(app)
        .post('/api/photos/upload')
        .attach('image', textFilePath);
      
      expect(response.status).toBe(400);
      
      // Clean up
      fs.unlinkSync(textFilePath);
    });
  });

  describe('GET /api/photos/profile/:profileId', () => {
    it('should get photos for a profile', async () => {
      // First upload a photo
      const uploadRes = await request(app)
        .post('/api/photos/upload')
        .attach('image', testPhotoPath)
        .field('profileId', 'photo-test-profile');
      
      uploadedFilename = uploadRes.body.photo.filename;

      const response = await request(app).get('/api/photos/profile/photo-test-profile');
      
      expect(response.status).toBe(200);
      expect(response.body.success).toBe(true);
      expect(Array.isArray(response.body.photos)).toBe(true);
    });

    it('should return empty array for profile with no photos', async () => {
      const response = await request(app).get('/api/photos/profile/non-existent-profile');
      
      expect(response.status).toBe(200);
      expect(response.body.photos).toEqual([]);
    });
  });

  describe('DELETE /api/photos/:filename', () => {
    it('should delete a photo', async () => {
      // First upload a photo
      const uploadRes = await request(app)
        .post('/api/photos/upload')
        .attach('image', testPhotoPath)
        .field('profileId', 'delete-test');
      
      const filename = uploadRes.body.photo.filename;

      const response = await request(app).delete(`/api/photos/${filename}`);
      
      expect(response.status).toBe(200);
      expect(response.body.success).toBe(true);
      
      // Verify file is deleted
      const filePath = path.join(__dirname, '../uploads/photos', filename);
      expect(fs.existsSync(filePath)).toBe(false);
    });
  });
});
